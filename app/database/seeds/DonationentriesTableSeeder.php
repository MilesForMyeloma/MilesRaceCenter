<?php

class DonationentriesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('donationentries')->truncate();

		$donationentries = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('donationentries')->insert($donationentries);
	}

}
